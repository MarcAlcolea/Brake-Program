# Core engine has no third-party runtime deps (pure Python + stdlib).
# The tooling below is for GUI, plots, reports, and tests — pinned loosely for now.

# GUI
PySide6>=6.6
pyqtgraph>=0.13

# Plotting / reports
matplotlib>=3.8
reportlab>=4.0

# Dev / test
pytest>=8.0

# Import of reference material (dev only)
openpyxl>=3.1
python-docx>=1.1
